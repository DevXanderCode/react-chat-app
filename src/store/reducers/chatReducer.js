const defaultState = {
	socket: null,
	message: '',
	threads: [],
	multipleThread: ''
};

const chat = (state = defaultState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default chat;
